{
  "name": "uniswap-sdk-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:lib": "turbo run build --filter='!@acme/*'",
    "dev": "turbo run dev",
    "dev:lib": "turbo run dev --filter='!@acme/*'",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,mjs,md,json,css}\"",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish",
    "anvil": "anvil --fork-url https://mainnet.gateway.tenderly.co --chain-id 1 --mnemonic-seed-unsafe 1337 --block-time 4"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.30.0",
    "@changesets/config": "^3.1.3",
    "eslint": "catalog:",
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "turbo": "^2.9.3"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24"
  }
}
