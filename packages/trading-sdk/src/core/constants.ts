export const DEFAULT_BASE_URL = "https://trade-api.gateway.uniswap.org/v1";
export const DEFAULT_AUTO_SLIPPAGE = "DEFAULT" as const;
export const DEFAULT_ROUTING_PREFERENCE = "BEST_PRICE" as const;
export const DEFAULT_PROTOCOLS = ["V2", "V3", "V4"] as const;
export const SUPPORTED_ROUTINGS = ["CLASSIC", "WRAP", "UNWRAP"] as const;
